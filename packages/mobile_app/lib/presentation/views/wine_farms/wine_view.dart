import 'package:flutter/material.dart';

class WineView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("wine view"),
    );
  }
}
